h1 Test #{@test.title}

p Level: #{@test.level}

h3 Questions

p = link_to 'Add question', new_test_question_path(@test)

table.table
  thead
    tr
      th Body
      th(colspan="3") Actions
  tbody
    = render @test.questions

p = link_to 'Back', tests_path
